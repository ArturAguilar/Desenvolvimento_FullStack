#include <iostream>

int main() {
    int contador = 1;

    // Estrutura de repetição while
    while (contador <= 5) {
        std::cout << "Contador: " << contador << std::endl;
        contador++; // incrementa o contador
    }

    return 0;
}